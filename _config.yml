# Site configuration for DESH / joindesh.in
title: "#desh>_ · Corruption is gravity. We're building the rocket."
description: "DESH is a software-defined think tank building a political party. We rebuild public services from first principles, for anywhere. We don't try to abolish the gravity of corruption. We engineer a system that reaches escape velocity anyway. First test bed: India."
url: "https://joindesh.in"
baseurl: ""
lang: en

# Build settings
markdown: kramdown
highlighter: rouge
permalink: pretty

# SEO and social
author: "DESH"
twitter:
  username: joindesh
  card: summary_large_image

# Collections for organized content
collections:
  primitives:
    output: true
    permalink: /primitives/:name/

# Plugins (all supported on GitHub Pages)
plugins:
  - jekyll-sitemap
  - jekyll-feed
  - jekyll-seo-tag

# Exclude from processing
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - .sass-cache
